# M2 Halation verification

Artifact: `m2-halation-2026-08-12`

| Scenario | Invocation | Observable result |
|---|---|---|
| Affected arm64 bundle build | `make build` | exit 0 |
| Common CPU/actual Metal contract | `make test-m1` | exit 0; `headless_render_contract: PASS (CPU + Metal M1 contract)` |
| Halation CPU/Metal parity and queue completion | `make test-m2` | exit 0; `halation_render_contract: PASS (CPU + Metal M2 Halation)` |

The focused suite submits to the supplied host queue, waits only on its same-queue sentinel, and checks CPU/Metal parity within `2e-4` together with alpha, HDR, hidden-RGB, render-window, identity, shape, and resolution fixtures.

## Direct re-verification (2026-08-12)

Commands were executed from `/Users/younseongmin/Documents/CBEF/DaVinciFilmPlugin`:

```text
make build && make test-m1 && make test-m2
```

Result: exit 0. The suites printed:

```text
headless_render_contract: PASS (CPU + Metal M1 contract)
halation_render_contract: PASS (CPU + Metal M2 Halation)
```

A forced rebuild and test run was also executed:

```text
make -B build && make -B test-m1 && make -B test-m2
```

Result: exit 0. The forced arm64 bundle compile/link completed, followed by the same two PASS lines above. A production-source scan found all four Halation kernels (`prepare`, `horizontal`, `vertical`, `finalize`), host-queue command-buffer commits returning `Enqueued`, and no temporary diagnostic strings in `src/metal/MetalRenderBackend.mm`. Ticket 03 is `resolved` and all seven acceptance boxes are checked; QA rows explicitly map Criteria 1–7.
