# Legacy experiments

`LightroomHalation/`은 CBEF Filter 이전의 Lightroom 할레이션 실험 원본입니다. 현재 OpenFX 제품의
빌드·설치·검증에는 참여하지 않으며 아이디어와 초기 구현 이력 보존용입니다.

Swift `.build`, clang/index 캐시와 과거 렌더 이미지는 재생성 가능하므로 보존하지 않습니다.
현재 제품 코드는 루트 `src/`, `tests/`, `fixtures/`만 기준으로 합니다.
